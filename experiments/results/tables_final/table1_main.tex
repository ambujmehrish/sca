% Table 1 -- main zero-shot table. MEASURED ROWS ONLY: every number here comes from one
% evaluation environment, one budget, one protocol, so every column is directly comparable
% and BOLD = best in column, no exclusions. Author-reported numbers and the baseline
% masked-/LoRA- variants (which are not part of GRAM's or PMRL's published methods) live in
% the appendix table (table8_reported_and_variants.tex).
\begin{table}[t]
\centering
\caption{Zero-shot retrieval on MSR-VTT (T-VAS), R@1\,/\,R@10 in both directions.
\emph{Every row is measured by us in a single evaluation environment}: same VAST
checkpoint, same 150k-clip subset of VAST-27M, one epoch, and the same two-stage protocol
(dual-encoder scoring followed by ITM cross-encoder reranking of the top~50), so the
columns are directly comparable and \textbf{bold} is the best value in each column. The
environment is anchored by evaluating the officially released GRAM checkpoint, which
scores 52.5 here against 54.8 as published (the same weights score 53.4 in a third
environment), so absolute values are not comparable to published tables; the
author-reported numbers are given in Appendix~Tab.~\ref{tab:reported}. $\pm$: std over 3
seeds. SCA's gain is in the text-to-video direction, which its gallery-side centroid
objective optimises; video-to-text is at parity. SCA is the only row that uses adapters
and masked training; because neither is part of GRAM's or PMRL's published method we do
not fold them into the baseline rows, but we do report baselines equipped with both in
Appendix~Tab.~\ref{tab:reported} -- the strongest such variant, GRAM\,+\,LoRA, reaches
53.3, still 1.6 below SCA. HyperGRAM releases no code and its description is ambiguous
between two readings of the hyperbolic branch; the row is our best-effort reproduction of
the reading its experiments section supports, and no claim about their method is drawn
from it.}
\label{tab:main}
\small
\setlength{\tabcolsep}{5pt}
\begin{tabular}{llcrrrr}
\toprule
 & & & \multicolumn{2}{c}{Text $\rightarrow$ Video} & \multicolumn{2}{c}{Video $\rightarrow$ Text} \\
\cmidrule(lr){4-5}\cmidrule(lr){6-7}
Method & Adapter & Mask & R@1 & R@10 & R@1 & R@10 \\
\midrule
\multicolumn{7}{l}{\emph{Baselines, measured here}} \\
GRAM (released checkpoint) & full-FT & \xmark & 52.5 & 82.5 & 50.5 & \textbf{81.2} \\
GRAM (reproduced) & full-FT & \xmark & 52.4\,\tiny{$\pm$0.21} & 82.6 & 49.7\,\tiny{$\pm$0.46} & 80.9 \\
PMRL (reproduced) & full-FT & \xmark & 51.9 & 80.6 & 49.7 & 79.9 \\
HyperGRAM (reproduced) & full-FT & \xmark & 51.0 & 76.7 & \textbf{50.7} & 79.6 \\
\midrule
\multicolumn{7}{l}{\emph{Ours}} \\
SCA, base learning rate & LoRA & \cmark & 53.5 & 81.9 & 49.9 & 78.1 \\
\textbf{SCA} & LoRA & \cmark & \textbf{54.9}\,\tiny{$\pm$0.15} & \textbf{83.5} & 49.2\,\tiny{$\pm$0.53} & 76.2 \\
\bottomrule
\end{tabular}
\end{table}
