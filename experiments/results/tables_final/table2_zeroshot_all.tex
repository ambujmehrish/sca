% Table 2 -- zero-shot across the four transfer benchmarks, both directions (ITM R@1).
% MSR-VTT is Table 1 and is deliberately not repeated here. Every row is run by us in one
% environment, so BOLD = best per column with no exclusions; the author-reported numbers for
% these benchmarks are in the appendix (table9_zeroshot_reported.tex). Markers as Table 1.
\begin{table*}[t]
\centering
\caption{Zero-shot retrieval away from the selection benchmark, R@1 (T2V\,/\,V2T).
MSR-VTT is Table~\ref{tab:main}. $^{\dagger}$ trained by us; all rows share one environment
and the 150k budget, so \textbf{bold} is the best per column (ties bolded jointly). Rows~1
and~2 are the \emph{recipe-matched} pair -- same learning rate, batch, schedule and budget,
differing only in the alignment objective -- and SCA wins six of the eight columns there,
ties one, and loses one. Row~3 is the MSR-VTT-tuned operating point, which buys in-domain
gain (Tab.~\ref{tab:main}) at the cost of transfer. $^{\ast}$ our VATEX evaluation uses the 431-clip audio-complete subset, not
the standard split, so its column is internally comparable but not comparable to published
VATEX numbers (App.~Tab.~\ref{tab:zs_reported}).}
\label{tab:zs_all}
\small
\setlength{\tabcolsep}{6pt}
\begin{tabular}{lcccc}
\toprule
Method & DiDeMo & ActivityNet & VATEX$^{\ast}$ & AudioCaps \\
 & T-VA & T-VA & T-VAS & T-VA \\
\midrule
GRAM$^{\dagger}$ & 49.6\,/\,\textbf{48.5} & 52.0\,/\,46.2 & 88.9\,/\,\textbf{86.1} & 33.1\,/\,32.8 \\
SCA (lr $2{\times}10^{-5}$) & 50.0\,/\,47.3 & \textbf{52.6}\,/\,\textbf{48.3} & \textbf{90.3}\,/\,\textbf{86.1} & \textbf{35.2}\,/\,\textbf{34.4} \\
SCA (lr $1{\times}10^{-4}$) & \textbf{50.2}\,/\,45.4 & 51.8\,/\,43.6 & 89.3\,/\,84.2 & 32.8\,/\,30.1 \\
\bottomrule
\end{tabular}
\end{table*}
