__pycache__/
*.pyc
.pytest_cache/
# repo-root run outputs / symlinks into SCRATCH-FAST (anchored: results elsewhere, e.g.
# experiments/results, ARE tracked)
/results
/workdir
/workdir_pretrain
workdir*/
/pretrained_weights
pretrained_weights/
/datasets
slurm_scripts/logs
smoke_gpu_*.out
wandb/
*.pt
# exception: the tiny t-SNE feature dumps ARE deliverables (rendered into the paper figure)
!experiments/results/tables_final/tsne_feats/*.pt
experiments/a10_workdir/
experiments/*.log
# upstream VAST/GRAM data-loader package (cluster-local, never committed here);
# repo-owned data modules are explicitly un-ignored
data/*.py
!data/semantic_targets.py
!data/mask_sampler.py
